import { HeroContent } from "@/components/hero-content";

export function Hero() {
  return (
    <section className="flex min-h-[85vh] items-center justify-center py-16">
      <HeroContent />
    </section>
  );
}
